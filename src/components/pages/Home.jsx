import styled from "styled-components";

const StyledMain = styled.main`
    min-height: calc(100vh - 200px);
`;

const Home = () => {
    return ( 
        <StyledMain>

        </StyledMain>
    );
}
 
export default Home;